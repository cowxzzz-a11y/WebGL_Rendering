# Sheet Packer

双击 `open_sheet_packer.vbs` 打开工具。

默认布局是广告板 8 角度：

- 第一行：`0001` 正前、`0002` 斜前、`0003` 正侧、`0004` 斜后
- 第二行：`0005` 正后、`0006` 斜后、`0007` 正侧、`0008` 斜前

使用方式：

1. 选择多张图片，或选择一个图片文件夹。
2. 工具会按文件名自然排序。
3. 调整列、行、单格尺寸、间距、背景等参数。
4. 点击 `导出 PNG`。

浏览器出于安全限制不会自动读取旁边的 `办公室` 文件夹，需要在界面里手动选择文件夹或图片。
